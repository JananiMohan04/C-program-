#include <stdio.h>
#include <string.h>
int main() {
    char pass[20];
    printf("Enter password: ");
    scanf("%s", pass);
    if(strlen(pass) < 6) 
        printf("Weak password\n");
    else 
        printf("Valid password\n");
    return 0;
}
