#include <stdio.h>
int main() {
    int total, present;
    printf("Enter total students: ");
    scanf("%d", &total);
    printf("Enter students present: ");
    scanf("%d", &present);
    printf("Attendance: %d/%d (%.2f%%)\n", present, total, (float)present/total*100);
    return 0;
}
